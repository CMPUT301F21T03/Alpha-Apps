<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prototypehabitapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PrototypeHabitApp">
        <activity android:name=".Activities.LogIn"/>
        <activity android:name=".Activities.HabitDetails"/>
        <activity android:name=".Activities.AddHabitEvent"/>
        <activity android:name=".Activities.EditHabitEvent"/>
        <activity android:name=".Activities.HabitEventDetails"/>
        <activity android:name=".Activities.FollowingFollowers"/>
        <activity android:name=".Activities.SignUp"/>
        <activity android:name=".Activities.Main"
            android:windowSoftInputMode="adjustNothing"/>
        <activity
            android:name=".Activities.BootScreen"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>